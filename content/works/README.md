# content/works

One JSON file per work, named by its `id`. Schema and validation rules: `docs/content-schema.md`. Editorial rules: `docs/editorial-policy.md`. Never edit `public/data/` by hand; run `npm run build:manifest`.
